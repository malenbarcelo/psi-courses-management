let eg = {
    formatter:new Intl.NumberFormat('es-ES', {
        minimumFractionDigits: 0,
        useGrouping: true
    }),
    idCompanies:0,
    idUsersCategories:0,
    data:[],
    dataFiltered:[]
}

export default eg